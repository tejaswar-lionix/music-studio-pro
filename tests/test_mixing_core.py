"""Tests for mixing core distinct"""

def test_mixing_core_0():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0

def test_mixing_core_1():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0

def test_mixing_core_2():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0

def test_mixing_core_3():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0
