"""Tests for mixing edge distinct"""

def test_mixing_edge_0():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0

def test_mixing_edge_1():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0

def test_mixing_edge_2():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0

def test_mixing_edge_3():
    gain=-3.0
    bounded=min(0.0, gain)
    assert bounded <= 0.0
