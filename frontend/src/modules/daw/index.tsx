import React, {useState} from 'react';
export const DawView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>DAW - DAW core - tracks, clips, arrangement, t</h2><p>tracks</p></div>
};
export default DawView;
